{
  "name": "gems",
  "version": "1.0.0",
  "description": "A mined, cited, searchable corpus of implementation patterns from open-source agent/research repos - queryable over MCP",
  "type": "module",
  "license": "MIT",
  "private": true,
  "engines": { "node": ">=18" },
  "scripts": {
    "test": "node --test",
    "sync": "node scripts/sync-corpus.mjs",
    "eval": "node scripts/eval-retrieval.mjs",
    "eval:uplift": "node scripts/eval-uplift.mjs --dry-run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0"
  }
}
